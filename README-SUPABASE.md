# Генератор звітів про дрони - Інтеграція з Supabase

## 🚀 Налаштування Supabase

### Крок 1: Створення проекту в Supabase

1. Перейдіть на [https://supabase.com](https://supabase.com)
2. Створіть обліковий запис або увійдіть
3. Натисніть "New Project"
4. Заповніть деталі проекту:
   - Project Name: `drone-reports` (або будь-яка назва)
   - Database Password: (збережіть надійний пароль)
   - Region: виберіть найближчий до вас регіон
5. Натисніть "Create new project" та зачекайте ~2 хвилини

### Крок 2: Створення таблиць

1. У Supabase Dashboard відкрийте **SQL Editor**
2. Натисніть "New Query"
3. Скопіюйте вміст файлу `supabase-schema.sql` та вставте у редактор
4. Натисніть "Run" або `Ctrl+Enter`
5. Створіть нову query
6. Скопіюйте вміст файлу `supabase-data.sql` та вставте у редактор
7. Натисніть "Run" або `Ctrl+Enter`

### Крок 3: Отримання API ключів

1. У Supabase Dashboard перейдіть до **Settings** > **API**
2. Знайдіть розділ "Project API keys"
3. Скопіюйте:
   - **Project URL** (схожий на `https://xxxxxxxxxxxxx.supabase.co`)
   - **anon/public key** (довгий ключ, який починається з `eyJ...`)

### Крок 4: Налаштування додатку

1. Відкрийте файл `supabase-config.js`
2. Замініть значення:
   ```javascript
   const SUPABASE_URL = 'YOUR_SUPABASE_URL'; // Вставте ваш Project URL
   const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'; // Вставте ваш anon key
   ```
3. Збережіть файл

### Крок 5: Тестування

1. Відкрийте `index.html` у браузері
2. Перевірте консоль браузера (F12) - має бути повідомлення:
   - `✅ Supabase client ініціалізовано`
   - `✅ Дані завантажено з Supabase`
3. Створіть тестовий звіт
4. Перевірте, чи з'явилася кнопка "Показати історію"
5. Натисніть її та переконайтесь, що звіт збережено

## 📁 Структура файлів

```
reportGenerator/
├── index.html                 # Основний HTML файл
├── script.js                  # Основна логіка додатку
├── style.css                  # Стилі
├── supabase-config.js         # ⚙️ Конфігурація Supabase (НАЛАШТУЙТЕ ТУТ!)
├── supabase-functions.js      # Функції для роботи з Supabase
├── reports-history.js         # Історія звітів
├── supabase-schema.sql        # SQL для створення таблиць
├── supabase-data.sql          # SQL для заповнення даних
├── data.json                  # Fallback дані (якщо Supabase недоступний)
└── README-SUPABASE.md         # Цей файл
```

## 🔄 Як це працює

1. **Завантаження даних**: При запуску додаток спершу намагається завантажити дані з Supabase. Якщо не вдається, використовується `data.json`, а якщо і це не працює - вбудовані fallback дані.

2. **Збереження звітів**: Після генерації звіту він автоматично зберігається в базу даних Supabase.

3. **Перегляд історії**: Кнопка "Показати історію" дозволяє переглядати, копіювати та видаляти раніше створені звіти.

## 🛠️ Можливості

- ✅ Збереження всіх звітів у хмарній базі даних
- ✅ Перегляд історії звітів
- ✅ Пошук та фільтрація звітів
- ✅ Копіювання звітів з історії
- ✅ Видалення звітів
- ✅ Автоматичний fallback на локальні дані
- ✅ Пагінація для великої кількості звітів

## 🔒 Безпека

Поточна конфігурація дозволяє всім користувачам читати і писати дані. Для production використання:

1. Увімкніть Row Level Security (RLS) у Supabase
2. Налаштуйте автентифікацію користувачів
3. Створіть політики доступу відповідно до ваших потреб

## 🐛 Troubleshooting

### Помилка "Supabase client не ініціалізовано"
- Перевірте, чи правильно вказані URL та ключ у `supabase-config.js`
- Переконайтесь, що ви зберегли файл після змін

### Помилка завантаження даних
- Перевірте, чи виконано SQL скрипти (`supabase-schema.sql` та `supabase-data.sql`)
- Перевірте консоль браузера на наявність помилок
- Переконайтесь, що RLS політики налаштовані правильно

### Звіти не зберігаються
- Перевірте консоль браузера на наявність помилок
- Переконайтесь, що таблиця `reports` створена
- Перевірте політики доступу для таблиці `reports`

## 📝 Примітки

- Додаток працює і без Supabase, використовуючи локальні дані
- Всі дані зберігаються у вашому проекті Supabase
- Безкоштовний план Supabase дозволяє до 500MB даних та 2GB трафіку на місяць

## 🔄 Оновлення даних у Supabase

Якщо потрібно додати нові опції (нові БК, оператори, тощо):

1. Перейдіть до **Table Editor** у Supabase Dashboard
2. Виберіть потрібну таблицю (наприклад, `bk_options`)
3. Натисніть "Insert row"
4. Заповніть поля `value` та `label`
5. Натисніть "Save"
6. Оновіть сторінку у браузері - нові опції з'являться автоматично

Або використайте SQL:
```sql
INSERT INTO bk_options (value, label) VALUES ('Новий БК', 'Новий БК');
```

## 📞 Підтримка

Для питань та допомоги:
- [Supabase Documentation](https://supabase.com/docs)
- [Supabase Community](https://github.com/supabase/supabase/discussions)
