# 🚀 Швидкий старт - Supabase інтеграція

## ✅ Що було зроблено

1. ✅ Додано Supabase JS SDK через CDN
2. ✅ Створено конфігураційні файли
3. ✅ Створено SQL скрипти для бази даних
4. ✅ Оновлено логіку завантаження даних (Supabase → JSON → Fallback)
5. ✅ Додано автоматичне збереження звітів у базу
6. ✅ Створено UI для перегляду історії звітів
7. ✅ Додано функції пошуку, фільтрації, копіювання та видалення

## 📋 Наступні кроки для запуску

### 1️⃣ Створіть проект у Supabase (5 хвилин)

1. Перейдіть на https://supabase.com та створіть акаунт
2. Створіть новий проект (Project Name: `drone-reports`)
3. Зачекайте ~2 хв поки проект створюється

### 2️⃣ Створіть таблиці в базі даних (2 хвилини)

1. Відкрийте **SQL Editor** у Supabase Dashboard
2. Скопіюйте вміст файлу `supabase-schema.sql` → вставте → Run
3. Скопіюйте вміст файлу `supabase-data.sql` → вставте → Run

### 3️⃣ Налаштуйте конфігурацію (1 хвилина)

1. У Supabase Dashboard: **Settings** > **API**
2. Скопіюйте:
   - **Project URL** (наприклад: `https://abc123.supabase.co`)
   - **anon public key** (довгий ключ: `eyJ...`)
3. Відкрийте файл `supabase-config.js`
4. Замініть:
   ```javascript
   const SUPABASE_URL = 'YOUR_SUPABASE_URL'; // Ваш Project URL
   const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'; // Ваш anon key
   ```
5. Збережіть файл

### 4️⃣ Запустіть та протестуйте

1. Відкрийте `index.html` у браузері
2. Відкрийте консоль (F12) - має бути:
   - ✅ `Supabase client ініціалізовано`
   - ✅ `Дані завантажено з Supabase`
3. Створіть тестовий звіт
4. Натисніть "Показати історію" - звіт має з'явитись у списку

## 🎯 Нові функції

### Автоматичне збереження
Кожен згенерований звіт автоматично зберігається в Supabase

### Історія звітів
- Перегляд всіх створених звітів
- Пошук за номером, підрозділом, оператором
- Фільтрація по підрозділу
- Пагінація (10 звітів на сторінку)

### Дії зі звітами
- 👁️ Переглянути - показує звіт у форматі як після генерації
- 📋 Копіювати - копіює текст звіту в буфер обміну
- 🗑️ Видалити - видаляє звіт з бази (з підтвердженням)

## 🔄 Fallback система

Додаток працює у 3 режимах (автоматичний вибір):

1. **Supabase** (пріоритет) - дані з хмарної бази
2. **data.json** (запасний) - якщо Supabase недоступний
3. **Fallback** (вбудований) - якщо JSON не завантажується

## 📁 Нові файли

- `supabase-config.js` - Ваші API ключі (НЕ коммітити!)
- `supabase-config.js.template` - Шаблон конфігурації
- `supabase-functions.js` - Функції для роботи з базою
- `supabase-schema.sql` - SQL для створення таблиць
- `supabase-data.sql` - SQL для початкових даних
- `reports-history.js` - UI історії звітів
- `README-SUPABASE.md` - Детальна документація
- `.gitignore` - Виключає supabase-config.js з Git

## 🔐 Безпека

⚠️ **ВАЖЛИВО**: Файл `supabase-config.js` містить ваші API ключі!
- Він вже додан в `.gitignore`
- НЕ коммітьте його в Git
- НЕ діліться ним публічно

## 💡 Поради

1. Безкоштовний план Supabase:
   - 500MB даних
   - 2GB трафіку/місяць
   - Цього достатньо для тисяч звітів

2. Додавання нових опцій:
   - Через **Table Editor** у Supabase
   - Або через SQL: `INSERT INTO bk_options (value, label) VALUES ('New', 'New');`

3. Резервне копіювання:
   - Supabase автоматично робить бекапи
   - Можна експортувати дані через SQL Editor

## 🐛 Проблеми?

Перевірте консоль браузера (F12) на наявність помилок.

Типові помилки:
- `Supabase client не ініціалізовано` → перевірте URL та ключ
- Помилки завантаження даних → перевірте чи виконані SQL скрипти
- Звіти не зберігаються → перевірте RLS політики у Supabase

Детальніше в `README-SUPABASE.md`

## ✨ Готово!

Тепер ваш додаток працює з хмарною базою даних! 🎉
