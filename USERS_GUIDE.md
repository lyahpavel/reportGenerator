# 📋 Інструкція: Додавання користувачів через Supabase

## Спосіб 1: Через Supabase Dashboard (Простіший)

1. **Відкрий Supabase Dashboard:**
   - Перейди на https://supabase.com
   - Відкрий проект `reportGenerator`

2. **Перейди до Authentication:**
   - Ліва панель → `Authentication` → `Users`

3. **Додай нового користувача:**
   - Клік на `Add user` → `Create new user`
   
4. **Заповни дані:**
   - **Email:** `username@local.app` (де `username` - це логін користувача)
   - **Password:** Пароль користувача (мінімум 6 символів)
   - **Auto Confirm User:** ✅ Увімкнути (щоб не треба було підтверджувати email)

5. **Додай метадані (необов'язково):**
   - Після створення, відкрий користувача
   - Перейди до `User Metadata`
   - Додай JSON:
   ```json
   {
     "username": "назва_користувача",
     "full_name": "Повне ім'я"
   }
   ```

## Спосіб 2: Через SQL Editor (Швидший для багатьох користувачів)

1. **Відкрий SQL Editor:**
   - Supabase Dashboard → `SQL Editor`

2. **Виконай SQL команду:**

```sql
-- Створення одного користувача
SELECT auth.create_user(
  email := 'username@local.app',
  password := 'password123',
  email_confirmed := true,
  user_metadata := jsonb_build_object(
    'username', 'username',
    'full_name', 'Ім''я Прізвище'
  )
);
```

### Приклад створення кількох користувачів:

```sql
-- Користувач 1
SELECT auth.create_user(
  email := 'pavlo@local.app',
  password := 'password123',
  email_confirmed := true,
  user_metadata := jsonb_build_object(
    'username', 'pavlo',
    'full_name', 'Павло'
  )
);

-- Користувач 2
SELECT auth.create_user(
  email := 'ivan@local.app',
  password := 'password456',
  email_confirmed := true,
  user_metadata := jsonb_build_object(
    'username', 'ivan',
    'full_name', 'Іван'
  )
);
```

## 📝 Формат даних

- **Email формат:** `логін@local.app`
  - Приклад: якщо логін `john`, то email `john@local.app`
  
- **Username:** 
  - 3-20 символів
  - Тільки літери, цифри, `_` або `-`
  - Приклад: `john`, `user_123`, `test-user`

- **Password:**
  - Мінімум 6 символів
  - Може містити будь-які символи

## 🔐 Як користувач входить

Після створення акаунту, користувач входить так:
- **Логін:** `username` (БЕЗ `@local.app`)
- **Пароль:** той що ти задав

Приклад:
- Створив: `pavlo@local.app` з паролем `mypass123`
- Користувач вводить:
  - Логін: `pavlo`
  - Пароль: `mypass123`

## ✅ Перевірка

Після створення користувача:
1. Відкрий сайт
2. Введи логін (без `@local.app`)
3. Введи пароль
4. Має увійти успішно!

## 🗑️ Видалення користувача

Через Dashboard:
1. `Authentication` → `Users`
2. Знайди користувача
3. `...` → `Delete user`

Або через SQL:
```sql
DELETE FROM auth.users WHERE email = 'username@local.app';
```

## 🔄 Зміна паролю

Через SQL:
```sql
-- Зміна паролю користувача
UPDATE auth.users 
SET encrypted_password = crypt('новий_пароль', gen_salt('bf'))
WHERE email = 'username@local.app';
```
